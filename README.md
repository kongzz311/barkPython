# Usage

add a py file called `keys.py` in the folder 

```python

keys = {
    '<keyName1>' : '<key1>',
    '<keyName2>' : '<key2>',
    '<keyName3>' : '<key3>',
}

```

run it by call the function `post` in sentBark.py